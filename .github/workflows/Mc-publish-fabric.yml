name: MC-Publish-Fabric (CurseForge/Modrinth)

on: [workflow_dispatch]

env:
  MINECRAFT_VERSION: 1.20, 1.20.1
  MOD_VERSION: 1.5.1
  JAVA_VERSION: 17

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check Environment Variables
        run: env

      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: "temurin"
          java-version: 17

      - name: Make Gradle Wrapper Executable
        if: ${{ runner.os != 'Windows' }}
        run: chmod +x ./gradlew

      - name: Build
        run: ./gradlew clean build

      - name: Publish on Curseforge and Modrinth (Fabric)
        uses: Kir-Antipov/mc-publish@v3.3
        with:

          modrinth-featured: true
          modrinth-id: iDiIfZLX
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}

          curseforge-id: 848022
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}

          files: Fabric/build/libs/!(*-@(dev|sources|javadoc)).jar

          name: ""
          version: "${{env.MINECRAFT_VERSION}}-${{env.MOD_VERSION}}"
          version-type: release
          changelog-file: CHANGELOG.md

          dependencies: |
            trinkets(recommended)
            ftb-chunks-fabric(recommended)
            xaeros-minimap(recommended)
            xaeros-minimap-fair-play-edition(recommended)
            fabric-seasons(required)
            fabric-seasons-extras(required)

          loaders: fabric
          game-versions: |
            [1.20,1.20.2)
          java: "${{env.JAVA_VERSION}}"

